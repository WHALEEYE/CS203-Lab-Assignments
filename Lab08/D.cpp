#include <iostream>
using namespace std;

int func_a(int x)
{
    int pow = 10;
    long long sum = 0;
    while (x > 0)
    {
        x /= 10;
        pow *= 10;
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t, k, s;
    cin >> t >> k >> s;

    return 0;
}